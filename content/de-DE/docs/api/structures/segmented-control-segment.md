# SegmentedControlSegment-Objekt

* `label` String-(optional) der Text, der in diesem Element angezeigt werden soll
* `label` String-(optional) der Text, der in diesem Element angezeigt werden soll
* `enabled` Boolean-(optional) ob dieses Segment wählbar ist. Default: True